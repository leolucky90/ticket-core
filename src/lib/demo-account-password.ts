/**
 * Canonical password for Firebase-seeded demo tenants, BossAdmin cookie login,
 * and marketing copy. Must match `scripts/reset-firebase-data.mjs` (DEMO_PASSWORD).
 */
export const DEMO_ACCOUNT_PASSWORD = "123456";
