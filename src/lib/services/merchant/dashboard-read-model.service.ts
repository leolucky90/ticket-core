import "server-only";
import { getDashboardBundle } from "@/lib/services/commerce";

// Dashboard aggregate remains compatibility-backed for now, but routes should import it from here.
export { getDashboardBundle };
