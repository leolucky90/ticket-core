/**
 * Canonical implementation lives in `components/merchant/catalog/dimension-picker`.
 * Re-export here for feature modules that expect a `feature/product` path.
 */
export { DimensionPicker } from "@/components/merchant/catalog/dimension-picker";
